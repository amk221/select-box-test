import generateFoos from 'select-box-test/utils/generate-foos';
import { module, test } from 'qunit';

module('Unit | Utility | generate-foos', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = generateFoos();
    assert.ok(result);
  });
});
