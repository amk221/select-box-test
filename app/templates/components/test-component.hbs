<div class="test-component" {{on "click" this.toggle}}>
  {{#if this.show}}
    {{#each @options as |option|}}
      <TestComponent::Child>
        {{yield option}}
      </TestComponent::Child>
    {{/each}}
  {{/if}}
</div>